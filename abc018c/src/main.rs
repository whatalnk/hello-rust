use proconio::input;
use proconio::marker::Chars;

fn main() {
    input! {
        t: usize,
        c: usize,
        k: usize,
        s: [Chars; c]
    }
    let mut ans = 0;

    println!("{}", ans);
}
